/**
 * OB-228 — /configure/plans (Zone A rail landing). The Living Plan Surface.
 */
'use client';
import { PlanSurfaceShell } from '@/components/plan-surface/PlanSurfaceShell';

export default function PlansPage() {
  return <PlanSurfaceShell selectedId={null} />;
}
