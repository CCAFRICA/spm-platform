# HF-282: CANONICAL IDENTITY RESOLUTION — ONE AUTH USER, ONE PROFILE, ONE READER, OBSERVABLE REDIRECTS

**File:** `docs/hotfixes/HF-282_DIRECTIVE_20260610.md` · **Branch:** `hf-282-identity-canon` off current `main` · **PR:** `--base main`

> Directive delivered inline (2026-06-10); committed here verbatim per Rule 29 / DD-8.
> Full execution evidence: `docs/completion-reports/HF-282_COMPLETION_REPORT_20260610.md`.

## §1 — PROBLEM STATEMENT (summary)
Architect disposition (binding): one auth user maps to exactly one `profiles` row; role
inheritance is a presentation concern, never duplicate identity rows (SOC 2 CC6). Defect
class: post-auth identity resolution has no canonical read (four divergent read shapes —
`.maybeSingle()` at middleware:304 errors on >1 row, the DIAG-060 account-keyed fork) and
provisioning has no canonical writer (torn provisioning — auth.users without profiles).
13 redirect branches emit no platform_event (silent ejection). One class: reader, writer,
data constraint, gate, and observability ship as one slice (SR-34 / Vertical Slice).

## §3 — PHASES
- **Phase 0:** reads + FP-49 live-schema evidence (profiles columns/constraints/indexes,
  duplicate census, orphan census, FK reference check). HALT-3 if a tenant-loading guard
  already prevents the auth-shell race.
- **Phase 1:** canonical reader `resolveIdentity()` (no single/maybeSingle; deterministic
  winner; alias-normalized via resolveRole; identity.resolve.* anomalies). Replace the
  divergent reads. EPG-1 greps.
- **Phase 2:** tenant-gate race fix (hydration-aware) + redirect observability (the 13-branch
  inventory each emits a named event). EPG-2 greps.
- **Phase 3:** canonical writer `provision-user.ts` (ensureAuthUser + exactly one profile,
  abort-loud); orphan repair; supersede fix-sabor-users.ts. EPG-3.
- **Phase 4:** data reconciliation + `UNIQUE(auth_user_id)` migration (CC authors; ARCHITECT
  applies). HALT-2 if duplicate census returns groups beyond platform@.
- **Phase 5:** deterministic tests + full regression + build gate.
- **Phase 6:** SR-39 compliance verification (SOC 2 CC6 / OWASP / NIST 800-63B / DS-014).

## §4 — HALT CONDITIONS
HALT-1 un-re-pointable FK rows; HALT-2 duplicate groups beyond platform@; HALT-3 tenant-
loading guard already prevents the race; HALT-4 a consumer depends on multi-row semantics;
HALT-5 compliance fails.

## §6 — OUT OF SCOPE
CLT91 RBAC/persona gating; B3 substrate separation; Supabase Site URL config (§6A); tenant
cookie write-path hardening (must not weaken); Plan-3 grammar HF; MFA enrollment UX.

*(Verbatim directive text delivered inline in the HF-282 session; this file records it for the audit trail.)*
