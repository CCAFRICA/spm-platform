# OB-211 Comprehensive Remaining — Orchestration Plan

**Date:** 2026-06-15 · **Orchestrated by:** CC (ultracode) · **Gate:** #519 (WS7 Stage 1) on main (`0dc32609`). The security foundation (WS7-A + Stage 1) is complete.

**The principle (architect-set): one comprehensive effort — Simulate incorporated as a co-phase, signal-verification as a phase-function.** The easy keystone (nav) does NOT ship alone while the strategic Simulate work is deferred. They complete together under one orchestration. The #508-classifier verification is Phase D's grounding (already read — it COLLAPSES the Simulate scope to a convergence, not a new build).

## Four orchestrated movements

### 1. Opening verify fan-out (parallel read-only, before any build)
6 disjoint agents re-ground all four phases on current main (main moves): (a) nav state, (b) results-table double-gate + G1 read site, (c) `useDrillThrough` two inline states, (d) payroll export + hierarchy gap, (e) **the 5 `parseTiers` Simulate sites + the 2 cards + confirm the classifier is still un-imported**, (f) the classifier shape (`classifyComponentRegime` + `attainmentFields`). One verified map = the build scope. (Initial recon confirms: parseTiers at loader 594/734/835/1050/1091; the loader does NOT import `performance-regime`; `classifyComponentRegime` returns `{regime:1|2|3, attainmentFields}`.)

### 2. Build orchestration — keystone → parallel arms
- **KEYSTONE (first): Phase A nav reorganization** — defines where surfaces live (the results-table placement references it).
- **Then 3 PARALLEL ARMS (disjoint file trees):**
  - **Arm 1 — Phase B verified fixes:** results-table gate+G1 ∥ `useDrillThrough` extraction ∥ export hierarchy (sub-parallel; `useDrillThrough` extracts before the table drill consumes it).
  - **Arm 2 — Phase D Simulate convergence (file-disjoint from A/B → genuinely parallel):** a MULTI-SITE coordinated pass — a shared `regimeForComponent` router drives ALL five `parseTiers` sites + the 2 cards identically (the failure mode is converging four and leaving one on `parseTiers`). EXTENDS HF-293 (regime-3 intact), ADDS regime-2 close-the-gap (live on BCL) + regime-1 volume-action.
  - **Arm 3 — Phase C RLS plan:** read-only authoring; runs anytime.
- **Disjointness (the basis for parallel):** Phase D touches the loader's Simulate path (`computeOptimizationOpportunities`/`buildRepData`) + `SelfSimulateCard`/`PopulationWhatIf`; Phase A touches `workspace-config`/`navigation` + the sidebar render sites; Phase B touches `operate/results` + `calculation-service`. Disjoint → parallel branches.

### 3. The batched adversarial sweep — a parallel LENS fan-out (not a checklist)
One fan-out over A/B/D, each lens an agent hunting a bug CLASS across every surface the batch touched (the form that caught wrong-rule-set / attainment-vs-dollars / empty-scope-means-all):
- **SR-39 scope (ELEVATED):** the fixes + regime-driven Simulate don't widen scope.
- **Finance entitlement (ELEVATED):** denied at route (WS7-A) AND menu (Phase A).
- **SR-38 Simulate-math (ELEVATED for D — bit 3×):** every regime anchors to the engine payout — regime-2 close-the-gap at the current attainment equals the persisted payout (delta=0 at rest); no fabricated dollar where the anchor fails. Hunts the class across ALL five converged sites.
- **Korean Test (ELEVATED for D):** routes on regime (structural) at EVERY site — never tiered-only, never a component-name literal.
- **scale (G1) + right-by-luck:** standard.
Every HIGH fixed at the CLASS layer (one general fix) + re-verified.

### 4. Merge strategy
Phase A (keystone) → {Arm 1 Phase B ∥ Arm 2 Phase D ∥ Arm 3 Phase C}, each architect-SR-44-gated, dependency-ordered. Never to main directly. The opening fan-out + the lens-sweep are paid ONCE across the whole remaining effort.

## Phase D — the convergence (the strategic phase, not deferred)

`classifyComponentRegime` (the #508 classifier) ALREADY provides the structural signal. Simulate currently keys on `parseTiers` ("is there a tiers array") → dead on BCL's regime-2 (attainment, no tier gate). Converge onto the classifier:
- **Regime 3** (gating/tierStructure) → "cross the boundary" — HF-293's tiered slider. UNCHANGED.
- **Regime 2** (tracked, `attainmentFields`) → "close the target gap" — project payout toward 100% attainment; SR-38-anchored (the #515 `sf` reconciliation generalizes: at current attainment = persisted payout, delta=0). The NEW path that makes Simulate live on BCL.
- **Regime 1** (no target) → the volume/relative action, NOT a fake slider (honest absence).
SR-34: one signal (the classifier), not a `parseTiers`-vs-regime divergence. **HALT-SIM-REGIME:** if `attainmentFields` is unrecoverable (composed-operand DAG) → regime-1 treatment + report; no fabricated target/gap. **DS-008-A3:** each what-if NAMES the action (Thermostat/IAP), leading into Recognize/Coach/Intervene.

## Not abandoned (§9)

WS-2 inc-2b (consumes the Phase-B `useDrillThrough`), WS-3 (consumes it), WS-4/5/6 (the action-proximity surfaces Phase D's "name the action" leads into), `WhatIfSlider` i18n debt, the RLS follow-on, the demo `profile_scope` seeding + scope-cache invalidation — all on the board.

---

*OB-211 Comprehensive Orchestration · 2026-06-15 · vialuce.ai · one effort, Simulate incorporated, signal-verification as a phase-function, nothing displaced by the easy-executable.*
