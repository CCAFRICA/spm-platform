# OB-209: MAKE THE SUBSTRATE CANONICAL — ONE LIBRARY (PRIMITIVES) + LEVERAGE THE EXISTING SIGNAL FOUNDATION

**Repo:** `CCAFRICA/spm-platform` (VP)
**Authored:** 2026-06-14 (architect channel)
**Type:** OB — substrate canonicalization. Consolidate the Decide surfaces onto ONE visualization library (`design-system/` primitives — no new vocabulary, no registry), REMOVE the inferior parallel paths, and EXTEND the EXISTING canonical signal foundation (`writeSignal` + the existing interaction-capture pattern) to the Decide surfaces so meaningful interactions are captured-and-reacted-to. **Builds nothing new where a working system exists.** One PR.
**Number:** OB-209 — highest merged OB is 207; OB-208 consumed by open PR #509; next free is 209 (verified from repo). **Collision gate (Phase 0, mandatory):** CC runs `ls docs/vp-prompts/OB-209* docs/completion-reports/OB-209*` and `git log --all --oneline | grep -iE 'OB-209'`; if any match, HALT. **Dependency:** builds on OB-208 #509; CC confirms #509 on main or HALTs.
**Branch + PR:** `ob-209-substrate-canonical`, PR to `main`. Never push to main directly.
**Governing specs:** DS-003 (the visualization primitives — applied, not re-vocabularied), Vialuce_Synaptic_State_Specification (Observation-IS-Action; the signal surface), DS-023 §5.1 (single canonical signal write path — `writeSignal`; the dual-architecture defect already closed), HF-219 (signal-registry ERADICATED — open-vocabulary signal_types, no registration gate), the three-flywheel read-coupling (AUD-004 L1/L2/L3), Decision 64 (Dual Intelligence — individual + universal on one substrate), the OB-207 Performance Regime ADR, OB-208 (#509), Korean Test, SR-34 (fix by removal), Bloodwork.

**THE DEFINING CONSTRAINT (architect-set): leverage, do not create.** A live code read establishes that the signal foundation ALREADY EXISTS and is ALREADY canonical:
- ONE write path: `@/lib/intelligence/canonical-signal-writer` (`writeSignal`/`writeSignalBatch`). DS-023 §5.1 single-entry-point; the parallel-insert dual-architecture defect was ALREADY found and CLOSED.
- Open-vocabulary signal_types (HF-219: "signal-registry eradicated; emission unconditional; no registration gate") — so a user-interaction signal needs NO new vocabulary or registry.
- Interaction capture ALREADY EXISTS: `import-interaction-signals.ts` defines `ImportInteractionAction = 'view'|'expand'|'action_click'|'correction'|'dwell'` with `captureImportInteraction()` + batched flush.
- Read path EXISTS: `api/signals` GET (by signal_type), `signal-reader.ts` `getTrainingSignals`, `classification-signal-service` `getSignals`, `flywheel-signal-emission` `emitFlywheelSignals`.

This OB does NOT create a signal-capture hook, a signal vocabulary, a registry, or a parallel library. It EXTENDS the existing interaction-capture to the Decide surfaces, writes through the existing `writeSignal`, and reads through the existing readers. Any phase that proposes building a new signal path or a new library has reintroduced the exact defect this OB exists to close.

---

## §0 — CC Standing Rules

Read `CC_STANDING_ARCHITECTURE_RULES.md` in full. Binding: AP-25 (Korean Test — open-vocabulary, structural; NO new registry), **SR-34 (fix by removing the inferior path; NEVER add a third)**, SR-38, SR-39, SR-41 (revert discipline), SR-42, SR-43, SR-44. Drafting source: `INF_Structured_Compliant_Drafting_Reference_20260513.md`.

**Read-before-assume (the lesson this OB embodies):** Before touching ANY component or signal path, CC reads the actual code — the `design-system/` exports, the `intelligence/` card imports, `canonical-signal-writer.ts`, `import-interaction-signals.ts`, `signal-reader.ts` — and pastes what exists. NO "build" where "apply/extend existing" is correct. NO new file where an existing service does the job.

**No new vocabulary/registry (architect-set):** apply `design-system/` primitives directly; the signal system already eradicated its registry (HF-219) — do not add one. signal_type is an open-vocabulary string written through `writeSignal`.

**FP-49:** before any signal write, CC reads `canonical-signal-writer.ts`'s `CanonicalSignalInput` interface + the `classification_signals` columns and writes through `writeSignal` ONLY (no `.from('classification_signals').insert` — that's the closed defect).

AUTONOMY: NEVER ask yes/no. Act. Commit + push after every phase. Git from repo root (`~/spm-platform`).

**Reconciliation-channel separation:** No ground-truth values.

**Skeleton/perceptible-unit rule:** completion gate (PG-FINAL) = both Decide surfaces render from the ONE library, inferior paths removed, AND a meaningful interaction writes through `writeSignal` and is readable back (individual + universal) on BCL. Do not stop after the audit or after one surface without removal.

---

## §1 — PROBLEM STATEMENT

### 1.1 The visualization library is bypassed; inferior paths accumulate (live-verified)

- `design-system/` holds 28 primitives (a superset of DS-003's nine: Sparkline, DistributionChart, BenchmarkBar, GoalGradientBar, BudgetGauge, AnomalyMatrix, CalculationWaterfall, etc.).
- The live /stream surface imports `@/components/intelligence` + `@/components/stream`, NOT `design-system/`.
- **ZERO of the 20 `intelligence/` cards import `design-system/`** — they reimplement inline (DistributionCard on raw recharts; others hand-rolled divs).
- Inferior duplicates coexist unremoved: `charts/` (CompensationPieChart — DS-003 forbids pie; `leaderboard` dup of `RelativeLeaderboard`; `goal-progress-bar` dup of `GoalGradientBar`), `analytics/` (KPICard dup).
- `BloodworkCard.tsx` exists but isn't surfaced ("Bloodwork not showing").

### 1.2 The signal foundation EXISTS and is canonical — it is simply not applied to the Decide surfaces (live-verified)

- ONE write path (`writeSignal`, DS-023 §5.1; dual-architecture defect closed), open-vocabulary (HF-219, no registry), interaction-capture already built (`import-interaction-signals.ts`), read path built (`api/signals` GET, `signal-reader`, `flywheel-signal-emission`).
- Only 2 files emit `stream_interaction`; the existing interaction-capture is wired to the IMPORT surface, not the Decide surfaces. The adaptive loop (capture → react: the expand/collapse learning) is specified (Synaptic spec: Observation-IS-Action) and the plumbing exists — it just isn't applied where you interact.

### 1.3 The defect class

Substrate-non-canonical: superior canonical paths (`design-system/`, `writeSignal` + interaction-capture) exist; the surfaces use inferior/no path. The fix is APPLY + REMOVE + EXTEND-EXISTING — never create.

---

## §2 — SUBSTRATE-BOUND DISCIPLINE

**DS-003 primitives (the one library):** `design-system/` is the single visualization source; surfaces compose from its primitives. No inline chart reimplementation. No new component vocabulary or registry — apply the primitives that exist.

**DS-023 §5.1 + HF-219 (the one signal path, open-vocabulary):** all signal writes go through `writeSignal`. signal_type is an open-vocabulary string (no registry — HF-219 eradicated it). A user-interaction signal is just a `writeSignal` call with an interaction signal_type. The dual-architecture defect (parallel insert) stays closed — no new write path.

**Synaptic spec — Observation IS Action:** the captured interaction signal's presence is the reaction. Capture-and-react is one mechanism: write the signal (capture); the adaptive default reads it back (react). The expand/collapse learning is this loop.

**Three-flywheel read-coupling (AUD-004 L1/L2/L3) + Decision 64 (Dual Intelligence):** the individual benefits from their OWN signals (L1 read — their expand/collapse history flips their default) AND the universal-insight agent reads ACROSS users (L3 aggregate — improvement insight). Both read the SAME canonical surface via the existing readers. Individual-direct and universal-aggregate are the L1-vs-L3 read of one signal store, not two systems.

**SR-34 (fix by removal):** inferior duplicate paths are removed once import-graph-proven unused. No wrapper, no third path.

**Korean Test:** primitives + signal_types are structural/open-vocabulary; no domain literals; no registry.

**Bloodwork:** surface the existing `BloodworkCard` (the "not showing" fix) + the Insight narrative + semantic-color health.

---

## §3 — PHASE 1: CANONICAL-PATH AUDIT + ADR (read the existing code, map, decide)

### 3.1 Read what exists (read-before-assume)

CC inventories + pastes the import/usage graph:
- `design-system/` exports (the primitives).
- Every `intelligence/`, `stream/`, `results/`, `charts/`, `analytics/` component: what it imports, what imports it. Which `intelligence/` cards reimplement inline a primitive that exists → map card → the `design-system/` primitive it should use.
- The dead duplicates (`charts/CompensationPieChart`, `charts/leaderboard`, `charts/goal-progress-bar`, `analytics/KPICard`) and whether anything live imports them.
- The signal foundation: `canonical-signal-writer.ts` (`writeSignal`, `CanonicalSignalInput`), `import-interaction-signals.ts` (the existing capture + actions), `signal-reader.ts`/`api/signals` GET (the read). Paste the interfaces — this is what gets EXTENDED, not rebuilt.

### 3.2 The canonical-path ADR

Commit `docs/architecture/CANONICAL_SUBSTRATE_ADR_OB209.md`:

```
DECISION: ONE visualization library + ONE signal path. Apply/extend existing; remove inferior; create nothing new.

  VISUALIZATION: design-system/ primitives are the single library (superset of DS-003's nine — apply them,
    no new vocabulary, no registry). intelligence/ cards COMPOSE FROM design-system primitives (not inline recharts).
  REMOVE (import-proven unused): charts/ (pie [DS-003-forbidden], dup leaderboard, dup goal-bar), analytics/ dups,
    inline reimplementations replaced by primitives. SR-34: repoint then delete; no wrapper, no third path.

  SIGNAL: the EXISTING canonical foundation is leveraged, not recreated:
    WRITE: writeSignal (DS-023 §5.1, dual-architecture defect already closed) — open-vocabulary signal_type (HF-219,
      no registry). User-interaction signal = a writeSignal call with an interaction signal_type.
    CAPTURE: extend the EXISTING import-interaction-signals pattern (view/expand/action_click/correction/dwell)
      to the Decide surfaces' meaningful interactions (drill, expand/collapse, action-card click).
    READ (capture-and-react / dual benefit): individual reads their OWN signals (L1) → adaptive default
      (the expand/collapse learning); universal-insight agent reads ACROSS users (L3) → improvement insight.
      Both via the EXISTING readers (api/signals GET, signal-reader). One surface, two reads (Decision 64 / L1-vs-L3).

  CREATE NOTHING: no new signal hook, no signal vocabulary, no registry, no parallel library. Apply + extend + remove.
  BLOODWORK: surface the existing BloodworkCard.
REJECTED: a new interaction-capture hook (import-interaction-signals exists); a signal registry (HF-219 eradicated it);
  a new/wrapper library (design-system exists); leaving charts/analytics dups.
```

### 3.3 Commit
`feat(OB-209): canonical-substrate ADR + read-verified map — one library (design-system primitives), one signal path (existing writeSignal+capture); the removal+extension plan`

---

## §4 — PHASE 2: REPOINT DECIDE CARDS TO design-system PRIMITIVES

Replace each Decide-surface `intelligence/` card's inline visualization with the `design-system/` primitive it should compose from (Phase 1 map): DistributionCard→`DistributionChart`; TrajectoryCard→`Sparkline`/trend; SystemHealthCard→HeroMetric-primitive+`LifecycleStepper`; OptimizationCard→PrioritySortedList-primitive+`WhatIfSlider`; component breakdown→`ComponentStack`/`BenchmarkBar`; TeamHeatmap/Coaching/Acceleration→their primitives; **surface `BloodworkCard`**. Cards keep their data role; visualization comes from the primitives. **SR-38:** any value rendered is trace-verified unchanged by repointing. **No new vocabulary** — primitives take data/labels via props.

**HALT-1:** a primitive lacks a capability the inline version had — EXTEND the primitive (canonical), do not keep the inline parallel.

### 4.1 Commit
`feat(OB-209): repoint Decide cards to design-system primitives — remove inline reimplementations; surface BloodworkCard`

---

## §5 — PHASE 3: REMOVE INFERIOR PATHS (SR-34, import-proven)

After Phase 2, for each dead-duplicate: grep-prove zero remaining imports, then `git rm` (SR-41 forensic trail). Targets: `charts/CompensationPieChart`, `charts/leaderboard`, `charts/goal-progress-bar`, `analytics/KPICard`+dups, replaced inline reimplementations. The visualization-dir count goes DOWN.

**HALT-2:** a removal candidate is still imported by a NON-Decide surface — repoint if cheap, else KEEP + note as removal residual; never break a live import.

### 5.1 Commit
`fix(OB-209): remove inferior duplicate visualization paths — import-proven unused (SR-34)`

---

## §6 — PHASE 4: EXTEND THE EXISTING SIGNAL CAPTURE TO THE DECIDE SURFACES (leverage, don't create)

### 6.1 Extend the existing interaction-capture (NOT a new hook)

Use the EXISTING `import-interaction-signals.ts` pattern (`captureImportInteraction` / `ImportInteractionAction` = view/expand/action_click/correction/dwell + batched flush) as the model, and either generalize that module or add the Decide-surface equivalent that writes through the SAME `writeSignal` canonical path. The Decide surfaces' meaningful interactions — drill/Verify, expand/collapse, action-card click (Investigate/Resolve/Simulate/Recognize/Coach), distribution-band click, filter/sort — emit an interaction signal via `writeSignal` with an open-vocabulary interaction signal_type and structural signal_value ({surface, card, interaction_type, persona, tenant}). Batched/debounced per the existing pattern. **NO new registry (HF-219); NO new write path (writeSignal only, DS-023 §5.1).**

**Capture bar (architect-set): DELIBERATE meaningful interactions on the Decide surfaces** — drill/expand/action — modeled on the existing import-interaction action set, not every DOM click.

### 6.2 Prove capture-and-react: the expand/collapse learning (Observation IS Action)

Wire the adaptive default as the REACTION to the captured signal, using the EXISTING read path:
- On collapse/expand of a Decide-surface section, `writeSignal` an interaction signal (capture).
- On surface load, read the user's OWN interaction signals (L1 — via the existing `api/signals` GET / `signal-reader` filtered to the user); if they've repeatedly collapsed a section, default it collapsed (react). The individual benefits directly from their own actions — the expand/collapse learning, exactly as specified.

### 6.3 Prove the universal read-basis (the insight agent foundation)

Confirm the SAME signals are readable ACROSS users (L3 aggregate) via the existing read path — a query returning interaction signals across users for a surface (the basis the universal-insight agent consumes). This OB establishes that the foundation supports BOTH the individual (L1) and universal (L3) read; the universal-insight AGENT itself is a follow-on that consumes this. (Decision 64: one substrate, two reads.)

**HALT-3:** the existing `writeSignal` rejects an interaction signal_type (it shouldn't — HF-219 open-vocabulary) — report the actual failure; do NOT create a parallel write path.

### 6.4 Commit
`feat(OB-209): extend existing interaction-capture to Decide surfaces via writeSignal — expand/collapse learning (L1 react) + universal read-basis (L3), no new signal path`

---

## §7 — PHASE 5: BUILD + VERIFY

### 7.1 SR-39
Signal writes via `writeSignal` (tenant-scoped, the canonical path); repoint/removal touches no auth surface; reads tenant-scoped. Confirm SOC2 CC6, DS-014, Decision 123. **HALT-4:** a read/write crosses tenant scope.

### 7.2 Build
Kill dev server. `rm -rf .next`. `npm run build`. Zero new errors. `npm run dev`. localhost:3000.

### 7.3 Proof gates — rendered result + source value, pasted

| PG | Specification | PASS |
|---|---|---|
| PG-01 | Canonical-substrate ADR + read map | ADR + the read-verified import/usage graph + the existing signal interfaces (writeSignal, import-interaction-signals, signal-reader). Paste. |
| PG-02 | Decide cards compose from design-system | grep: Decide cards import `@/components/design-system`; DistributionCard no longer raw recharts. Before/after. |
| PG-03 | No new library/vocabulary/registry | grep proves no new component dir, no signal registry added (HF-219 honored), no new signal write path (writeSignal only). |
| PG-04 | Inferior paths REMOVED | charts/pie+dups, analytics dup `git rm`'d; zero-import proof BEFORE removal. Dir count DOWN. |
| PG-05 | BloodworkCard surfaced | BloodworkCard renders on the Admin Decide surface. Screenshot. |
| PG-06 | Capture via existing writeSignal | A Decide-surface drill/expand/action writes an interaction signal THROUGH `writeSignal` (paste the row + confirm the call path is writeSignal, not a new insert). |
| PG-07 | Expand/collapse learning (capture-and-react, L1) | Collapse a section → signal written; reload → the user's own history defaults it collapsed. Show the signal + the reacted default. |
| PG-08 | Universal read-basis (L3) | A read returns interaction signals ACROSS users for a surface (the insight-agent foundation) via the existing read path. Show the query + result. |
| PG-09 | Values unchanged by repoint | A repointed component's value matches pre-repoint (SR-38). Trace one. |
| PG-10 | Korean Test / open-vocabulary | grep: primitives + interaction signal_type structural/open-vocabulary; no registry, no domain literals. Paste + zero-hit. |
| PG-11 | Build exit 0 | `npm run build` exit 0, warning-clean; no broken imports from removals. |
| PG-FINAL | **Both Decide surfaces from ONE library, inferior paths gone, capture-and-react live (completion gate)** | BCL admin → /stream AND /results render from `design-system/` primitives (verify DistributionChart, Sparkline, surfaced BloodworkCard); inferior dups gone from the tree; a collapse writes via writeSignal and the reload reacts (L1); the cross-user read works (L3). Architect SR-44 confirms; CC provides render + signal row + removal proof + the react behavior. |

Paste evidence for every PG.

### 7.4 Commit + PR
```bash
gh pr create --base main --head ob-209-substrate-canonical \
  --title "OB-209: Make the substrate canonical — one library (primitives) + leverage the existing signal foundation" \
  --body "Closes substrate-non-canonical on the Decide surfaces by APPLYING and EXTENDING what exists, building nothing new. VISUALIZATION: design-system/ primitives are the one library (no new vocabulary/registry); Decide cards repointed to compose from them (DistributionChart/Sparkline/ComponentStack/LifecycleStepper/WhatIfSlider); inline reimplementations and inferior duplicate paths (charts/ pie [DS-003-forbidden]+dups, analytics/ dups) REMOVED (SR-34, import-proven — dir count down); BloodworkCard surfaced (the 'Bloodwork not showing' fix). SIGNAL: leverages the EXISTING canonical foundation — writeSignal (DS-023 §5.1, the dual-architecture defect already closed), open-vocabulary signal_type (HF-219, no registry), the existing import-interaction-capture pattern EXTENDED to the Decide surfaces. Capture-and-react proven via the expand/collapse learning (individual reads their OWN signals → adaptive default, L1) and the universal read-basis confirmed (cross-user read → insight-agent foundation, L3) — one substrate, two reads (Decision 64). NO new signal hook, registry, write path, or library. Builds on OB-208 #509. SR-44: architect confirms both surfaces from one library, inferior paths gone, capture-and-react live."
```

---

## §8 — HALT CONDITIONS

- **HALT-1:** a design-system primitive lacks a capability the inline had — extend the primitive (canonical); no inline parallel.
- **HALT-2:** a removal candidate is still imported by a non-Decide surface — repoint if cheap, else keep + note residual; never break a live import.
- **HALT-3:** `writeSignal` rejects an interaction signal_type — report the real failure; do NOT create a parallel write path.
- **HALT-4:** a signal read/write crosses tenant scope.
- **HALT-5:** any locked rule (Korean Test, SR-34, DS-023 §5.1, HF-219 no-registry, Bloodwork, Decision 123) conflicts. Surface verbatim per SR-42.
- **HALT-GENERAL:** if any phase would require CREATING a new signal path, registry, vocabulary, or library — STOP. The existing system is the path. Report what was about to be created and why the existing system doesn't suffice.

---

## §9 — REPORTING DISCIPLINE

Completion report: `docs/completion-reports/OB-209_COMPLETION_REPORT_20260614.md`

Per Rules 25-28: SHA per phase, the ADR + read-verified import/usage graph + the existing signal interfaces leveraged, before/after card imports, zero-import proof + removals, the interaction signal row (THROUGH writeSignal) + the L1 react + the L3 cross-user read, value-unchanged trace (SR-38), pasted evidence for every PG (esp. PG-FINAL), SR-39, build, PR URL. Explicitly confirm: nothing new was created (no hook, registry, write path, library) — existing systems applied/extended.

```
ARTIFACT SYNC
MC: substrate-non-canonical (library bypassed, inferior paths unremoved, Bloodwork not surfaced, signal foundation not applied to Decide) → CLOSED on the Decide surfaces by OB-209 (pending SR-44). Built nothing new — leveraged design-system + writeSignal + existing interaction-capture.
REGISTRY: "Canonical Visualization Library" → design-system primitives, one path, inferior removed. "Signal Foundation Applied" → existing writeSignal+interaction-capture extended to Decide; capture-and-react (L1 individual) + universal read-basis (L3). "Bloodwork" → surfaced.
R1: Tier C candidate "one library (primitives); no inferior paths; capture-and-react on the existing signal foundation" → pending SR-44.
BOARD: Decide (surfaces canonical + Bloodwork + capture-and-react); Platform Core (existing signal foundation applied).
SUBSTRATE: design-system canonical (SR-34 removal, read-verified); the EXISTING canonical signal foundation (writeSignal DS-023 §5.1, HF-219 open-vocabulary, existing interaction-capture) extended — Observation-IS-Action capture-and-react, L1 individual + L3 universal (Decision 64 dual read); NOTHING created.
```

---

## §10 — OUT OF SCOPE

- The universal-insight AGENT itself (this OB establishes the L3 read-basis; the agent that consumes it for universal improvement is a follow-on).
- Full app-wide interaction capture (this OB extends the existing capture to the Decide surfaces; other surfaces follow).
- Adaptive surface ORDERING (consumes the capture; follow-on).
- OB-208 Pass-2 narrative/re-skin work beyond what consolidation surfaces.
- Per-persona Manager/Rep/Financial/cockpit surfaces (compose from the now-canonical library).
- Engine 🟠/🔴; OB-201; DS-027.

## §10A — RESIDUALS

- **R1 — The universal-insight agent.** The L3 cross-user read-basis is established; the agent reading all user signals for universal improvement (your note) consumes it. Follow-on — and it builds on THIS foundation, not a new one.
- **R2 — App-wide capture.** The existing interaction-capture is extended to the Decide surfaces; generalizing to every surface is a follow-on.
- **R3 — Adaptive ordering.** Capture-and-react proven for expand/collapse (L1); fuller adaptive ordering (Synaptic full vision) consumes the same surface.
- **R4 — Remaining inferior paths.** Duplicates still imported by non-Decide surfaces (HALT-2) noted for a later pass.
- **R5 — Non-Decide library adoption.** 17/128 pages use design-system; the rest is a continuing effort.
- **R6 — R1 Exit Criteria.** On SR-44, propose updates. One-way ratchet.
