# OB-225: Decision 158 Pipeline Completion

## §0 CC Standing Rules

Read `CC_STANDING_ARCHITECTURE_RULES.md`. This directive references `INF_Structured_Compliant_Drafting_Reference_20260513.md` for drafting discipline. AP-25, SR-34, SR-35, SR-38, SR-41 binding throughout.

**IRA brief (binding, IRA invocation ded357c9):** 11 substrate entries bound. Three supersession candidates identified (E910 construction boundary, E902 round-trip closure, E906 signal-accumulation contract). Core finding: the LLM currently performs BOTH recognition AND construction (Decision 158 violation); three remediation attempts (OB-222, OB-223, OB-223-R2) loaded engine-vocabulary guidance into the prompt, which is registry propagation (E907 violation, E920 pattern). The structural response: complete the existing intent-transformer so deterministic code constructs the DAG.

## §1 Problem Statement

The plan interpretation pipeline has a designed three-stage architecture:

1. **Recognition (LLM):** reads plan document, produces `compositional_intent` -- a structured declaration of what the plan means
2. **Construction (intent-transformer):** deterministic code that converts `compositional_intent` into a `prime_dag` -- a tree of engine primitives
3. **Evaluation (prime DAG evaluator):** deterministic code that evaluates the DAG against resolved data

Stage 3 is proven correct (BCL $312,033, 510/510, byte-identical across 8 merged PRs). Stage 2 exists (`intent-transformer.ts`, 444 lines) but is bypassed -- the LLM produces the `prime_dag` directly, skipping the transformer. Stage 1's prompt has been progressively loaded with engine-vocabulary guidance (OB-222, OB-223, OB-223-R2) to compensate for the bypassed transformer, creating a registry pattern.

Empirical evidence (MIR, 5 plans, January 2025, reconciled against `MIR_Resultados_Esperados.xlsx`):

- P1: 1,104,032 vs GT 593,117 (1.86x) -- LLM produced duplicate components + flat rate without category filtering
- P2: 0 vs GT 210,000 -- temporal_map produced but resolver has no consumer (hardcoded vocabulary)
- P3: 219,632 vs GT 148,306 -- formula correct (17/17 match), eligibility gate omitted
- P4: 1,759,800 vs GT 8,850 (199x) -- counted all entity rows instead of filter(Verificado=Si) then count
- P5: 0 vs GT 0 -- correct for January

The LLM correctly recognized every plan's intent (metadata proves it). It incorrectly constructed the engine DAG because it does not understand the engine's computational model. This is the Decision 158 violation the IRA identified.

The resolver's hardcoded consumption paths (sum, snapshot, distinct_count) create vocabulary-coupling between convergence and the resolver. temporal_map was produced but has no consumer. Silent $0 on unrecognized bindings violates E910 (structured failure required, never silent fallback).

## §2 Substrate-Bound Discipline

**Decision 158 (LLM recognizes; deterministic code constructs):** The LLM stops producing `calculationIntent` / `prime_dag`. It produces ONLY `compositional_intent`. The intent-transformer reads the intent's structural properties and constructs the prime_dag deterministically.

**E910 (Korean Test, construction boundary extension):** The transformer derives from the canonical primitive declaration (`prime-grammar.ts`). It does not maintain a private copy of the vocabulary. Observable, named, structured failure on unrecognizable intent shapes -- never silent fallback. The LLM prompt contains NO engine primitive vocabulary (filter, aggregate, compare, etc.).

**E907 (Fix Logic Not Data):** No prompt-vocabulary additions. No workarounds. Complete the designed pipeline.

**E902 (Carry Everything, round-trip closure):** Every binding type convergence produces must be consumable by the resolver. The resolver executes structural specifications, not enumerated types.

**E906 (Closed-Loop Intelligence):** The transformer reads prior construction validation signals before constructing. Validated intent-to-DAG mappings accumulate across imports.

**E903 (No Hardcoded Assumptions):** The resolver's enumerated consumption paths are eliminated. Bindings carry structural specifications.

**Regression anchors (reconciliation-channel, architect-verified only):**
- BCL: $312,033 (6 periods, 510/510 SR-38)
- Meridian: $556,985
- MIR Plan 3 (Cobranza): 219,632 PEN January (the formula is correct; the gate is a separate concern)

## §3 Phase 1 -- Diagnostic (read-only, no code changes)

**Objective:** Map the exact current state of the three pipeline stages. Every subsequent phase depends on this inventory.

### §3.1 Read the interpreter output pipeline
Document where the LLM produces compositional_intent, where it produces calculationIntent (the prime_dag that bypasses the transformer), and the exact JSONB shape of compositional_intent for each of the 5 MIR plans.

### §3.2 Read the intent-transformer
Document what intent shapes the transformer handles, the fallback path when the LLM's calculationIntent is valid, and what structural properties the transformer reads.

### §3.3 Read the resolver's consumption paths
Document where the hardcoded consumption types are enumerated, how resolveColumnFromBatch dispatches on reduction, and where temporal_map fails.

### §3.4 Read the signal infrastructure
Document what signal types the PlanComprehensionEmitter writes and whether any signal_type captures validated DAG constructions.

### §3.5 Commit the diagnostic
Commit message: `"OB-225 Phase 1: pipeline diagnostic"`. File: `docs/diagnostics/OB-225_PIPELINE_DIAGNOSTIC.md`.

**HALT-DIAG:** If the intent-transformer handles MORE shapes than documented in this directive, or if the resolver has consumption paths beyond sum/snapshot/distinct_count, report the full inventory before proceeding.

## §4 Phase 2 -- Separate Recognition from Construction

The LLM produces only compositional_intent. The intent-transformer produces the prime_dag. The LLM prompt contains no engine primitive vocabulary.

### §4.1 Complete the intent-transformer
The transformer reads STRUCTURAL PROPERTIES of the compositional_intent, not type labels. Handle any intent that declares: categorized rates; threshold gate; count of qualified rows; banded lookup; multiplicative modifier; temporal_adjustment. The transformer reads the JSONB structure to detect which properties are present. It does NOT switch on a type string. Unknown structures produce an observable IntentTransformError (E910), not silent fallback. Derives from prime-grammar.ts (canonical primitive declaration).

### §4.2 Modify the interpreter prompt
Remove ALL engine primitive vocabulary from the plan interpretation prompt (filter, aggregate, compare, reference, constant, prime_dag, PrimeNode, scope, scope_aggregate). The prompt tells the LLM to produce a compositional_intent in the PLAN'S OWN VOCABULARY. Vocabulary grounding (ALI -> Alimentos) happens at finalization (Phase 4), not interpretation.

### §4.3 Modify the intent-resolver
Always use the transformer to produce the prime_dag from the compositional_intent. The calculationIntent field is no longer produced by the LLM. Transformer errors propagate -- no silent fallback. validateIntent validates the transformer's output.

### §4.4 Unit tests
Test the transformer against the 5 MIR plan intent shapes and BCL plan intent shapes (regression).

**HALT-REGRESSION:** If any BCL plan's transformer-produced DAG differs structurally from its current prime_dag (verified against the live rule_sets), HALT. Report the divergence with both DAG shapes.

### §4.5 Commit
Commit message: `"OB-225 Phase 2: Decision 158 enforcement -- intent-transformer completion"`. Build must pass. BCL regression must hold.

## §5 Phase 3 -- Eliminate Resolver Vocabulary Coupling

New binding shape: `{column, aggregation: {op, ...params}, filter?: {field, operator, value}, period_key?}`. resolveColumnFromBatch reads aggregation.op and applies it (structural dispatch). Unknown op -> observable ResolutionError (E902). Legacy `{reduction}` bindings convert with a logged deprecation (documented migration path, not silent fallback). Byte-identical tests for sum/snapshot/distinct_count + temporal_map + count-with-filter.

Commit: `"OB-225 Phase 3: resolver vocabulary elimination (E902 round-trip closure)"`.

## §5A Phase 4 -- Binding Materialization at Finalization

Import finalization materializes bindings from stored signals (no LLM at calc time for binding discovery). Read comprehension + data-classification signals, match tokens to columns deterministically, produce vocabulary mapping (ALI -> Alimentos via SELECT DISTINCT), store materialized bindings + grounding signal. Calc-time LLM binding (OB-216 §2-S4) replaced by reading materialized bindings; LLM fallback is a logged degraded path.

Commit: `"OB-225 Phase 4: binding materialization at finalization"`.

## §5B Phase 5 -- Signal Accumulation

On COMMITTED, store construction_validation signal (intent_shape hash -> dag_shape hash, verified). Before constructing, the transformer queries for a validated construction for this intent shape and reuses the validated DAG structure (deterministic reuse).

Commit: `"OB-225 Phase 5: construction signal accumulation (E906 closed-loop)"`.

## §5C Phase 6 -- Verification

BCL regression: recalc must produce $312,033, 510/510 SR-38 byte-identical. Meridian regression: match the stored verified anchor ($556,985). `npm run build` exit 0. Open PR against main. Do not merge (SR-44). Completion report `docs/completion-reports/OB-225_COMPLETION_REPORT.md` per Rules 25-28 with ARTIFACT SYNC block.

## §6 Out of Scope
- MIR ground-truth reconciliation (architect-gated, post-merge)
- Plan 3 eligibility gate (plan-reading question, not construction)
- OB-214 (self-correcting interpreter agent)
- VG governance extensions
- Multi-period MIR calculation

## §6A Residuals
- OB-222/223/223-R2 prompt additions removed once the transformer handles the structural properties (E907 debt).
- intent-transformer.ts legacy lines preserved as compat path, not deleted; new components flow through structural-property construction.
- Convergence LLM fallback (§5A.3) is a degraded path; emit a flywheel signal so the IMA tracks finalization binding-materialization failures. Goal: zero calc-time LLM invocations for binding.
- Plan 3's eligibility gate may not be construction-related (OB-214 class if implicit from domain knowledge).

*File IS the prompt. Phase prose is the executable.*
