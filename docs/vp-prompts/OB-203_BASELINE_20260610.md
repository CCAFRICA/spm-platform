# OB-203 — Phase 0.5 Baseline Witness (Brasa y Maíz)

**Status: PENDING ARCHITECT FIXTURE.** `datos-cadena-restaurantes-mx.xlsx` (16 sheets, 162,956 rows)
is not present in the repo as of 2026-06-10 (searched: `*.xlsx` repo-wide — only
`test-fixtures/bcl/BCL_Resultados_Esperados.xlsx` and `web/scripts/CLT14B_Reconciliation_Detail.xlsx`).
Per directive §3.5 the architect supplies the file. This doc is updated with the LIVE capture on receipt.

## Documented failure signature (DS-027 §1 — the expectation the live capture must reproduce)
- One in-line header-comprehension LLM call covering all novel sheets ran ~68s and returned
  unparseable JSON → `[SCI] Header comprehension JSON parse failed (AIService fallback)`
  (`src/lib/sci/header-comprehension.ts:83`).
- Silent heuristic fallback: `llmCalled=false avgConf=0.00 cols=0 insights=0`
  (`analyze/route.ts:226` diag line; `header-comprehension.ts:204` fallback return).
- Employee roster (Empleados) misclassified as transaction @ ~76%.
- 16-unit proposal presented as comprehended (no `failed_interpretation` surface exists pre-OB).
- HF-247 quality gate blocks all fingerprint writes (`columnRoles contains 'unknown'` ×13)
  (`fingerprint-flywheel.ts:171`) → identical cold-start on every retry.
- Four stored pre-HF-247 fingerprints carry undefined roles and Tier-1 match forever.

## Live capture plan (on fixture receipt)
1. `npm run dev`; import the file via the SCI proposal path as a SANDBOX tenant (never a proof tenant — blast-radius §1).
2. Capture: the `[SCI]`/`[SCI-HC-DIAG]`/`[SCI-FINGERPRINT]` console stream; the resulting proposal
   (per-sheet classification + confidence); the HF-247 write-block log lines.
3. Paste verbatim here as the committed baseline artifact; this is the "before" against which the
   Phase 6 exit witnesses (correct classifications, zero-LLM re-import) are measured.
