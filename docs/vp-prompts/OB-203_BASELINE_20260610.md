# OB-203 — Phase 0.5 Baseline Witness (Brasa y Maíz) — BLIND-HOLDOUT

**Discipline (directive §2 L38, §3.5 L50, HALT-10):** the real tenant file
`datos-cadena-restaurantes-mx.xlsx` is **architect-held and architect-executed**. It is NEVER
committed to the repo and NEVER in CC's possession. CC captures the run's logs and signal/DB
state and records the file's **sha256 as reported by the architect**. A fixture in the repo
would convert the exit witness into a fitting target (tenant-solution risk) — code that
comprehends a structural analog it has never seen, in a vocabulary it has never seen, is
platform code by construction.

**Status:** capture tooling staged; awaiting the architect-executed run.

## Architect-executed capture protocol
1. **Architect** runs the import in dev (`localhost:3000`) against a **sandbox tenant** (never a
   proof tenant — blast-radius §1), uploading the real file through the SCI proposal path.
2. **Architect reports** to CC: (a) the sandbox `tenant_id` used; (b) the file `sha256`;
   (c) the dev console stream (the `[SCI]` / `[SCI-HC-DIAG]` / `[SCI-FINGERPRINT]` lines) — or
   confirms the dev stdout was tee'd to a logfile CC can read.
3. **CC captures** (no file contact): pastes the console stream; runs a service-role tsx read of
   the sandbox tenant's post-run DB state — `structural_fingerprints` (poisoned/zero-new writes
   under the HF-247 gate) and any `classification_signals` rows — and pastes the output here.
4. **CC commits** this artifact as the "before" baseline. It is the reference against which the
   Phase 6 exit witnesses (correct classifications, zero-LLM re-import) are measured — those exit
   witnesses are likewise architect-executed blind-holdout (§3.6 L101).

## Documented failure signature (DS-027 §1 — what the live capture must reproduce)
- One in-line header-comprehension LLM call over all novel sheets ran ~68s, returned unparseable
  JSON → `[SCI] Header comprehension JSON parse failed (AIService fallback)` (`header-comprehension.ts:83`).
- Silent heuristic fallback: `llmCalled=false avgConf=0.00 cols=0 insights=0`
  (`analyze/route.ts:226`; `header-comprehension.ts:204`).
- Empleados (employee roster) misclassified as transaction @ ~76%.
- 16-unit proposal presented as comprehended (no `failed_interpretation` surface pre-OB).
- HF-247 blocks all fingerprint writes (`columnRoles contains 'unknown'` ×13) (`fingerprint-flywheel.ts:171`).
- Four stored pre-HF-247 fingerprints carry undefined roles and Tier-1 match forever.

## Capture record — CAPTURED 2026-06-11 (architect-executed run; CC DB read)
- **sandbox tenant_id (from table):** `3d354bfa-b298-48dd-88a0-9f8c5a00be4e` — NOT pristine (6 prior fingerprints, created 2026-06-10 20:37), so this run resolved via prior signal, not cold-start.
- **file sha256 (architect-reported):** _pending_ (per disposition; recorded when reported).
- **console stream:** architect-witnessed; key reported facts incorporated below (CC holds no file, no raw console paste — DB read substitutes per blind-holdout).

### A. Client-timeout finding (DS-027 R2 evidence — architect-witnessed + CC-corroborated)
Two overlapping requests: **Run A 04:13:07**, **Run B 04:14:07** (fired during Run A's in-flight
comprehension call). Browser symptom: **FETCH FAILED** — client fetch timeout (~60s) on the
synchronous import request during the **67.3s blocking comprehension call**. Run A's proposal was
generated **server-side at 04:14:18 — after client death**; Run B is the retry.
**CC corroboration (DB):** Run A's 16 `classification:outcome` signals all carry `created_at`
≈ `04:14:18` (server-side, post-client-timeout); Run B's `Portada` signal at `04:15:19`. The
request-scoped session produced its outcome **after** the client that asked for it had died — the
R2 defect (request-scoped session cannot deliver its own outcome) made empirical.

### B. Misclassification (DS-027 §1 signature reproduced — now a LEARNED prior)
All 16 sheets resolved `decision_source=prior_signal` (warm tenant). The roster mis-call is now
replayed from prior signal:
```
Empleados            -> transaction  conf=0.7555   <-- employee roster misclassified as transaction (~76%)
Ventas_Transaccional -> transaction  conf=0.7304
Productos_SKU        -> transaction  conf=0.2555
Resumen_* (10)       -> target/transaction, conf 0.24-0.74
Portada              -> reference     conf=0.2381
Sucursales           -> target        conf=0.7175
```
The wrong classification is not merely produced — it has been **learned and is being replayed**,
which is strictly worse than a one-off mis-call (flywheel poisoning).

### C. Fingerprint-store SCOPE (item 3 — verified from `structural_fingerprints`, not logs)
**TENANT-SCOPED.** The same structural hash exists as **separate rows per tenant with independent
match_counts** — it is not a global store:
```
hash afb789d55ae5be4e:
  tenant 3d354bfa-b298-48dd-88a0-9f8c5a00be4e  match_count=2  conf=0.6667   (the run tenant)
  tenant dbe3b308-1483-4cd8-8032-6fdd4a8a8f5c  match_count=1  conf=0.5
structural_fingerprints total rows (all tenants): 22 ; run tenant holds 6.
```
The `matchCount=2` Tier-1 matches in the run came from the run tenant's OWN prior imports (the read
gate filters `.eq('tenant_id', tenantId)` — `fingerprint-flywheel.ts:43-49`). Phase 2 builds atom
granularity on this same tenant-scoped store.

### D. HF-247 skip set
**Architect-console-reported:** 13 skips this run, incl. the Tier-1-matched `afb789d55ae5` — matches
production; baseline consistent. **CC DB read (as-found, no cleanup):** run tenant holds 6 stored
fingerprints, **0 currently carrying unknown roles** (skips are write-time events; blocked writes
leave no row). The afb789d55ae5 Tier-1 row stands at mc=2/conf=0.6667.

### E. Corrected Phase 1 expectation (per disposition item 4)
Phase 1's witness for this file class is **durable, verifiable failure SIGNALS** (queried from
`classification_signals`), not a Brasa-y-Maíz UI screenshot. UI rendering of `failed_interpretation`
is witnessed on a **small induced-failure structural analog**; Brasa-y-Maíz-class user-visible
rendering is a **Phase 2** witness.

## Phase 2-6 testing note (no real data)
All Phase 2-6 tests run against the **seeded structural-analog generator** (built in Phase 2,
committed as code): same structural class (cover sheet, entity roster, reference tables,
high-cardinality fact grain, derived aggregations) with randomized-token / non-Latin vocabulary,
never real tenant vocabulary, plus a cross-vocabulary generality test. EPG-2.2 is pointed at a
generated analog. The induced-failure exit witness (§3.6) uses a corrupted generated analog.
