# HF-228: PLATFORM DATA APERTURE COMPLETION

Predecessor: HF-226 (PR #403), HF-227 (PR #404), DIAG-048 (PR #405 + addendum).

Defect class addressed: pipeline data aperture incomplete for multi-source plans —
quota / target / reference data classified as `entity`, schema-collision in
`inventoryData` sampling, single-data-type-per-component gate in
`generateAllComponentBindings`, `metric_derivations` not executed in production
entity loop in `route.ts`, null safety bug in `resolveSource`.

Five surfaces (one per file):
1. `web/src/lib/sci/agents.ts` — referential-vs-definitional classification signal.
2. `web/src/lib/intelligence/convergence-service.ts:inventoryData` — schema-aware sampling.
3. `web/src/lib/intelligence/convergence-service.ts:generateAllComponentBindings` — cross-data-type column discovery.
4. `web/src/app/api/calculation/run/route.ts` — call `applyMetricDerivations` and merge `derivedMetrics`.
5. `web/src/lib/calculation/intent-executor.ts:resolveSource` — null guards on `field.startsWith(...)`.

Full directive text preserved verbatim in the conversation that initiated HF-228
work, 2026-05-16. This file marks the prompt commit per standing rule 5.
