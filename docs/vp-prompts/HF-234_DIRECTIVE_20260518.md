# HF-234: CONVERGENCE SEPARATION OF CONCERNS — COLUMN MAPPING vs METRIC DERIVATION

Predecessor: HF-114 (PR #217 convergence_mapping task type), HF-227
(PR #404 binding filter completion), HF-228 (PR #406 metric derivation
execution in production entity loop), HF-231 (PR #409 unified import
pipeline), HF-232 (PR #410 reference_key discrimination), HF-233
(PR #411 classification-aware entity_id_field).

Defect: CRP Plan 1 P1/P2 reconcile exact ($73,142.72 / $109,139.46);
P3/P4 fail ($110,269.14 / $94,700.74 vs GT $93,524.42 / $84,201.24).
`resolveColumnMappingsViaAI` non-deterministically returns either flat
string `{"metric": "column"}` (no filter) or object form
`{"metric": {"column":"col","filters":[...]}}`. When the AI returns
string form, the binding entry carries no filter and revenue across
all product classes is summed.

Fix surface (one file, ~20 lines):
- `web/src/lib/intelligence/convergence-service.ts`:
  1. Strip HF-227's categorical-context block from Call 1's prompt.
     Call 1 returns pure column mapping. Filter discovery is no longer
     its responsibility.
  2. Pass 4 (`generateAISemanticDerivations`) fires for ALL metrics when
     categorical data exists — not only those not yet in `derivations`.
     The existing single-trigger preserved when no categorical data.
  3. Verify the route.ts write path already writes both
     `convergence_bindings` AND `metric_derivations` unconditionally
     (route.ts:244-273; no edit needed).

The engine uses both structures: convergence_bindings for
role-keyed column resolution; metric_derivations for metric-name-keyed
filtered computation. They produce different keys; no conflict.

Full directive text preserved verbatim in the conversation that
initiated HF-234 work, 2026-05-18. This file marks the prompt commit
per standing rule 5.
