# HF-288 — ROSTER EMAIL SOURCING + PII CLEANSE SCHEDULER
**Repo path:** `docs/vp-prompts/HF-288_DIRECTIVE_20260613.md` · **Date:** 2026-06-13
**Closes:** OB-204 residuals 1 and 2 (roster email placeholder, ip-nulling scheduler)
**Number derivation:** architect-confirmed HF-288 as next free (HF-285–287 allocated). Phase 0 re-confirms HF-288 free against `docs/vp-prompts/` and repo; renumber-and-state if taken.

## §0 — CC STANDING RULES HEADER
`CC_STANDING_ARCHITECTURE_RULES.md` binds throughout. Binding for this directive: AP-25 (Korean Test — email field identified structurally, never by header-name literal), SR-34 (no bypass), SR-43 (ship completes the work item), Rules 25–28 (evidentiary reporting). Drafting-discipline source: `INF_Structured_Compliant_Drafting_Reference_20260513.md` (DD-1..DD-12; this file is the prompt per DD-11).

## §1 — PROBLEM STATEMENT
**Residual 1:** OB-204 Phase F's bulk promote (F11) uses a `@roster.invalid` placeholder email for each entity because the roster read endpoint does not surface the email value from the entity's imported data. The email address exists in `committed_data.row_data` — the import pipeline classified one field as email-semantic (via `classification_signals`) — but the roster endpoint doesn't join through to return it. Result: every bulk-promoted user gets a dead invite email; the admin must manually fill each one, defeating the bulk pattern.
**Residual 2:** The Q-I 90-day PII cleanse query (OB-204 Phase B, migration 017 item 6) is committed but runs only when manually executed. No scheduled automation exists. Historical PII in `platform_events.payload` accumulates until someone remembers to run it.
Combined treatment: both are small, share no code, but close the last two named OB-204 residuals in one work item.

## §2 — SUBSTRATE-BOUND DISCIPLINE APPLICATIONS
- **T1-E910 / AP-25 (Korean Test):** the email field is identified by the import's structural classification (`classification_signals` where `classification` indicates email-semantic type, or `vocabulary_bindings` maps the field to an email semantic). NEVER by matching a header name like `'email'` or `'correo'`. The classification layer already did this work — this HF reads its output.
- **T1-E902 (CEEC):** committed_data carries ALL imported columns in `row_data`. The email value is read from `row_data` at the key identified by classification — no persistence-time narrowing occurred.
- **I-1 (uuid spine):** the `suggestedEmail` field travels from the API to the UI for display/prefill only. It is NOT stored in events — the lifecycle event for a bulk-promoted user records uuid + role + action, never the email (the existing I-1 emitter from OB-204 Phase A governs).

## §3 — PHASES

### §3.0 — PHASE 0: PRE-FLIGHT
- **0.1** Confirm HF-288 free: `ls docs/vp-prompts/ | grep -i "HF-288"` + `grep -ril "HF-288" docs/ | head -5`. If taken → renumber-and-state.
- **0.2** Locate the roster read path: `git grep -n "profile_id.*NULL\|roster\|without.*platform" -- 'web/src/' 'web/pages/' 'web/app/'` — identify the endpoint or query that returns entities without platform access (the F11 source). Paste the file + function.
- **0.3** Locate the email classification: `git grep -n "classification_signals\|vocabulary_bindings\|header_comprehension" -- 'web/src/'` — identify how the import pipeline stores field semantics. Paste the relevant type or query.
- **0.4** Verify committed_data linkage: confirm `committed_data` has a join path from entity_id to row_data for roster entities (entity_id FK + import_batch_id). Paste one sample row's `row_data` keys to confirm the email field key matches what classification identified.

### §3.1 — PHASE 1: ROSTER EMAIL SOURCING (branch `hf-288`)
- **1.1 Server-side resolution:** in the roster read endpoint (located at 0.2), extend the query: for each entity, LEFT JOIN `committed_data` (most recent row for that entity, data_type = roster/entity — verify the actual type at 0.4) → LEFT JOIN `classification_signals` for the same `import_batch_id` (or tenant-scoped, whichever the pipeline uses) where the classification indicates email-semantic → extract the value from `row_data` at the classified field key → return as `suggestedEmail` (nullable) on each entity in the API response.
  - The join must be structural: the classification record identifies the field key; the code reads `row_data[classifiedKey]` — no hardcoded key name anywhere (Korean Test).
  - If no classification exists or the field is empty → `suggestedEmail` = null (the UI falls back to manual fill, same as today minus the placeholder).
- **1.2 UI prefill:** in the bulk-promote dialog and single-entity promote (F7), prefill the invite email from `suggestedEmail` when present. The admin can still override. Remove the `@roster.invalid` placeholder — null/empty gets an empty field with a "No email found in import" hint, not a fake address.
- **1.3 Verification:** run the bulk-promote flow against a tenant with imported roster data. Assert: (a) entities with an email-classified field show the correct `suggestedEmail` from their import, (b) entities without show null/empty, (c) the promote succeeds using the suggested email (dry-run delivery per F-1 if RESEND_API_KEY absent). Paste output.
- **Build verification + commit. EPG:** `git grep -n "roster.invalid" -- 'web/'` → zero hits. `git grep -n "'email'" -- 'web/src/lib/entities/' 'web/src/lib/auth/provision'` → zero header-name literals in the sourcing path (Korean Test proof).

### §3.2 — PHASE 2: PII CLEANSE SCHEDULER (architect-applied, SR-44)
CC authors a migration file `supabase/migrations/<timestamp>_hf288_pii_cleanse_cron.sql`:
```sql
-- Enable pg_cron if not already (idempotent)
CREATE EXTENSION IF NOT EXISTS pg_cron;

-- Schedule weekly PII cleanse: Sunday 03:00 UTC
SELECT cron.schedule(
  'ob204-pii-cleanse-90d',
  '0 3 * * 0',
  $$UPDATE public.platform_events
    SET payload = payload - 'email' - 'ip_address' - 'ip' - 'user_agent'
    WHERE created_at < now() - interval '90 days'
      AND (payload ? 'email' OR payload ? 'ip_address' OR payload ? 'ip' OR payload ? 'user_agent')$$
);
```
CC commits the file. **Architect applies** via Dashboard SQL Editor. CC verifies: `SELECT * FROM cron.job WHERE jobname = 'ob204-pii-cleanse-90d'` via tsx script — paste the row showing schedule + command. If pg_cron is not available on the Supabase plan → HALT-1; record as a residual with manual-cadence workaround unchanged.

### §3.3 — PR
`gh pr create --base main --head hf-288 --title "HF-288: roster email sourcing from import classification + PII cleanse scheduler"`. Architect merges.

## §4 — HALT CONDITIONS
- **HALT-1:** pg_cron not available on the Supabase plan → record scheduler as still-manual residual; Phase 1 (email sourcing) is unaffected and ships independently.
- **HALT-2:** classification_signals does not contain an email-semantic classification for any existing import → paste the classification vocabulary; architect dispositions whether to extend the classifier or accept manual-fill as the interim.

## §5 — REPORTING DISCIPLINE
Completion report at `docs/completion-reports/HF-288_COMPLETION_REPORT_20260613.md`. Per Rules 25–28: Phase 0 evidence (pasted roster endpoint, classification structure, committed_data sample); Phase 1 verification output; Phase 2 cron.job query output; EPG outputs; SHA.

## §6 — OUT OF SCOPE
Extending the import classifier to recognize new email-semantic patterns; bulk-promote UX beyond email prefill; scope enforcement (RLS); any modification to the import/SCI pipeline itself.

## §6A — RESIDUALS
- If HALT-1 fires: scheduler remains manual-cadence until a plan upgrade or alternative scheduling mechanism.
- Classification vocabulary may not cover all import patterns — the classifier itself is a separate evolution path.

*HF-288 · roster emails sourced from data, not invented · PII cleanse automated*
*vialuce.ai · Intelligence. Acceleration. Performance.*
