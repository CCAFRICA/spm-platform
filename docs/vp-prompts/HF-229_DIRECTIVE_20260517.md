# HF-229: DECISION 108 ENFORCEMENT AT HC PATTERN LEVEL

Predecessor: HF-095 (PR #196, Decision 108 at content-profile.ts), HF-196 Phase
1B (Decision 108 at agents.ts), HF-228 (PR #406, platform data aperture).

Defect: Decision 108 (HC Override Authority, LOCKED 2026-03-07) was enforced at
the structural-detection layer and at the agent-scoring layer, but NOT at the
HC pattern matching layer in `synaptic-ingestion-state.ts`. The `entity_roster`
pattern locks classification at 90% from three structural conditions
(HAS identifier, HAS name, idRepeatRatio<=1.5) without consulting HC column
roles. Quota / target files matching those structural conditions misclassify
as `entity` even when HC identifies a `measure` column at >= 0.80.

Single surface (one file, ~10 lines):
- `web/src/lib/sci/synaptic-ingestion-state.ts` — add `hasMeasureColumn`
  precheck from HC column roles; gate `entity_roster` pattern with
  `&& !hasMeasureColumn`.

Full directive text preserved verbatim in the conversation that initiated
HF-229 work, 2026-05-17. This file marks the prompt commit per standing
rule 5.
