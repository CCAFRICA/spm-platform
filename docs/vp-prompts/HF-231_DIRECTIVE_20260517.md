# HF-231: UNIFIED IMPORT PIPELINE — commitContentUnit

Predecessor: HF-184 (PR #331, partial committed_data unification),
HF-194 (PR #370, partial field_identities alignment), DIAG-022
(PARALLEL_SPECIALIZED verdict), AP-17 tech debt.

Defect class: 8 inline committed_data write sites across 2 routes × 4
sub-pipelines drift independently. Four recurrences of "drifted sub-
pipeline" defect — fixed one at a time, then another drifts.

Fix surface (one new file + edits to two routes):
- `web/src/lib/sci/commit-content-unit.ts` — new shared function.
- `web/src/app/api/import/sci/execute-bulk/route.ts` — replace 4 inline
  writes with `commitContentUnit` calls.
- `web/src/app/api/import/sci/execute/route.ts` — replace 4 inline
  writes with `commitContentUnit` calls.

Side effects (entity creation in `entities` table, plan interpretation
AI call) are PRESERVED at the caller level. `commitContentUnit` writes
to `committed_data` only.

Full directive text preserved verbatim in the conversation that
initiated HF-231 work, 2026-05-17. This file marks the prompt commit
per standing rule 5.
