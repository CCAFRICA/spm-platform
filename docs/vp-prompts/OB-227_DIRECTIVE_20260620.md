# OB-227: Insights Workspace Remediation + Platform Experience Defects

**Date:** 2026-06-20 · **Type:** Operation Build · **Priority lane:** QUARTET (g2)
**MC:** #24/#39/#47/#B1 · **Backlog:** B.161–B.165 · **CLT:** CLT-226 · **R1:** E4, C1
**Drafted per:** `INF_Structured_Compliant_Drafting_Reference_20260513.md`

## §0 CC Standing Rules
AP-11 (no shell pages), AP-17 (single code path — ONE shared viz set for all Insights pages), SR-34 (fix structurally), SR-43 (ship=merge+verify+report+SHA), Rule 1 (Korean Test), Rule 7 (prove don't describe — live render + real data), Rule 10 (autonomy), Rule 14 (commit prompt), Vertical Slice Rule, Decision 92 (engine binds data to periods at calc time; periods come from `periods` table; every period surface reads it), Decision 93 (period is a calc parameter; `periods` table is SSOT).

## §1 Problem Statement — four defect clusters from CLT-226
- **A — Insights workspace empty/broken (6 of 7 sub-pages).** 7 Insights sub-pages; OB-224 wired drill-through into My Team. Compensation (/insights/compensation) renders frozen Oct-2025, no period selector, 100% donut for one plan, flat unsearchable entity table with useless "Components: 4" column, no drill-through, no search/filter. Other five (Overview, Analytics, Performance, Trends, Sales & Finance) likely stubs. Substrate is rich (BCL: 6 periods, 510 entity_period_outcomes, 510 calculation_traces, 120K committed_data). Gap is presentation.
- **B — Period mismatch.** /configure/periods shows 6 periods Oct-2025→Mar-2026 (correct). Lifecycle Cockpit /operate shows Sep-2025→Feb-2026 pills (March missing, September nonexistent). Root-cause + fix required; the Insights PeriodSelector must read canonical `periods` table.
- **C — No auto-detect period on Periods page.** `detectPeriods()` exists (import validation), `/api/periods/create-from-data` exists, but no button on /configure/periods calls them. Critical Blocker B1, still open.
- **D — Blank tenant landing.** New tenant sees blank "No periods configured" + one button. Replace with an onboarding checklist (empty tenant → first payout).

Combined-treatment rationale: all four share the period-query prerequisite, the same substrate, and the same goal. One PR (Vertical Slice).

## §2 Substrate-Bound Discipline
Korean Test (D154): labels from `periods.label`, names/currency from DB, zero hardcoded strings. Decision 92/93: every period surface reads `periods` WHERE tenant_id ORDER BY start_date — no deriving from source_dates, no hardcoded ranges, no uninvalidated caching. Decision 111 (Performance = rate+trajectory+reference+components, ratio/aggregate primitives, no ML). Decision 158 (deterministic, no AI calls). OB-224 dependency: consume DrillThroughPanel/EntityResultsList/ComponentCards/TransactionRows/DisputeInline — do NOT duplicate. Reconciliation-channel separation: render substrate, set no targets.

## §3 Phase 1 — Investigation + Period Fix
Inventory all Insights pages, Compensation, Lifecycle Cockpit, Periods config, Results Table; classify Empty/Partial/Functional + record data-fetch + charting patterns. Diagnose the Cockpit period mismatch root cause (committed_data.source_date vs periods table / date-window filter / derived key / boundary bug) and FIX THE ROOT CAUSE so Cockpit matches /configure/periods exactly. Extract canonical `getCalculatedPeriods(tenantId)` (read periods, join calculation_results, ORDER BY start_date DESC) used by every surface. Cross-period substrate verification SQL → `docs/diagnostics/OB-227_INVESTIGATION.md`. No halts in this phase.

## §3A Phase 2 — Intelligence Data Layer  (`web/src/lib/insights/`)
types.ts, periods.ts (getCalculatedPeriods), distribution.ts (getPayoutDistribution), component-totals.ts (getComponentTotals), trajectory.ts (getEntityTrajectory, getPopulationTrend), attainment.ts (getAttainmentSummary), entity-table.ts (getEntityTableData — paginated/filterable/sortable), tenant-state.ts (getTenantOnboardingState), index.ts. Types: PeriodSummary, DistributionResult/Bin, ComponentTotal, EntityTrajectory, EntityTableRow/Result, TenantOnboardingState — derive every field from a live DB row. Deterministic; verify with a test script. Commit.

## §3B Phase 3 — Shared Visualization Components  (`web/src/components/insights/`)
Identify existing charting lib first (recharts/etc.; don't add a new one). PeriodSelector, SummaryHero, DistributionChart, ComponentBars, TrendLine, EntityTable (search/filter/sort/server-pagination + OB-224 drill-through + CSV export, handles 10k entities), OnboardingChecklist (6 steps: plan→data→periods(+auto-detect)→calculate→results→approve, each ✓/●/○, plus 3 value-prop cards), InsightsLayout, index.ts. Korean Test throughout. Commit; `npm run build`.

## §3C Phase 4 — Platform Fixes + New Tenant Experience
§3C.1 Verify Cockpit period fix matches /configure/periods. §3C.2 Add "Auto-detect from data" button to /configure/periods (call detectPeriods/create-from-data against committed_data server-side; confirmation panel with labels/ranges/frequency/confidence → batch create → refresh; "No data uploaded yet" fallback). §3C.3 Render OnboardingChecklist on /operate when tenant in setup (full onboarding if no plan/data/periods; checklist+Cockpit if setup done but no calc; hide once calculations exist — preserve existing Cockpit for tenants with data). §3C.4 Results Table /operate/results: Option A wire to getEntityTableData + EntityTable + DrillThroughPanel + PeriodSelector, OR Option B redirect to /insights/compensation if redundant — choose from code. Commit; verify localhost.

## §3D Phase 5 — Insights Page Builds
Rebuild Compensation; build/rebuild Overview; build Analytics, Performance (Decision 111; attainment NULL→fallback), Trends (DS-015 §6 trajectory, all periods), Sales & Finance (financial lens, budget "not configured"); extend My Team (preserve OB-224, add PeriodSelector/SummaryHero/Δ/TrendLine). Each: InsightsLayout + PeriodSelector + SummaryHero + the page-specific charts + EntityTable + drill-through + interactions (bin/component click → filter table; trend click → change period; row → DrillThroughPanel). Commit; `npm run build`.

## §3E Phase 6 — Browser Verification (SR-43)
Kill dev → rm -rf .next → build → dev → verify localhost:3000. Platform fixes (period match, auto-detect, onboarding, results table). Each of 7 Insights pages: period selector, real data, charts, interactions, entity table, drill-through, fallbacks, no console errors. BCL cross-period: 6 periods Oct-Mar, TrendLine 6 points, trajectory arrows, component trends; March 2026 total = $58,406 / 85 entities (matches /operate/calculate). Record per-page in completion report.

## §4 HALT Conditions
- **HALT-1:** Cockpit period query uses a fundamentally different architecture than the `periods` table → HALT + record.
- **HALT-2:** `calculation_results.components` JSONB inconsistent across tenants → HALT + record both structures.
No other halts. Empty=clean slate. Broken=rebuild. Missing attainment=fallback. Missing budget="not configured".

## §5 Reporting
`docs/completion-reports/OB-227_COMPLETION_REPORT.md`: SHA, files+line counts, Cluster A/B/C/D findings+fixes, data layer + viz components (with prop interfaces), per-page verification, BCL 6-period results, Results Table disposition (A/B + rationale), premise corrections (faithfully). PR: `gh pr create --base main --head dev`.

## §6 Out of Scope
AI NL explanations; budget upload (renders "not configured"); gamification/goal-gradient/tier coaching; plan modeling/what-if; nav IA restructuring; acceleration intelligence; Financial Module pages; SOC2 UI; dispute workflow beyond OB-224 DisputeInline; mobile; lifecycle state-machine completion; entity hierarchy UI; profile_scope UI.

## §6A Residuals
R-1 attainment NULL→fallback. R-2 budget model future. R-3 trajectory needs 3+/4+ periods. R-4 component-name discontinuities. R-5 period-comparison population diffs. R-6 variant filter depends on resolved variant. R-7 Results Table may remain redirect. R-8 auto-detect depends on committed_data source_dates; manual create remains. R-9 detectPeriods may need server-side adaptation; don't ship a broken button. R-10 period fix may reveal same mismatch on other surfaces (/operate/calculate, /stream) — fix in this OB if found.

*OB-227 · File IS the prompt. No §7.*
